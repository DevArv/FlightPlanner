databases:
  - name: FlightPlanner-db

services:
  - type: web
    name: FlightPlanner-app
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: ConnectionStrings__DefaultConnection
        fromDatabase:
          name: FlightPlanner-db
          property: connectionString
